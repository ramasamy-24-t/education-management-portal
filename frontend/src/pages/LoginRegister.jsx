import PlaceholderPage from "../components/PlaceholderPage.jsx";

export default function LoginRegister() {
  return (
    <PlaceholderPage
      area="User Area"
      title="User Login / Register"
      items={["Account Access", "Role Selection (Student / Teacher)"]}
    />
  );
}
